# Description

By default we still print id,state,title.

However, give the user the option to print other columns:

E.g.:
- `git issue list` -- default
- `git issue list --column id,assignee,updated` -- specific columns
- `git issue list --column "*"` -- all columns


# Repro Steps

# Expected Behavior

# System Info
