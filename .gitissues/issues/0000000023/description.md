# Description

Lets add a configuration for the default columns to `config.yaml`

This will be applied whenever the user calls

```
git issue list
```

Currently, there is a hard coded default: `id,state,title`

Note, might make sense to add `assignee` to the default.

# Repro Steps

# Expected Behavior

# System Info
