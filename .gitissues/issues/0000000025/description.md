# Description

Might make sense to keep track of a list of allowed values for the enum types of the meta fields:

- `state`
- `type`
- `assignee`

Maybe for `labels` we want to be less strict?

The list of allowed options could be configurable in the `config.yaml`

# Repro Steps

# Expected Behavior

# System Info
